# Housekeeping

Registro de lo hecho, pendiente de limpieza y enlace a deuda técnica.

---

## Hecho recientemente

- **Fix `enrich-lead-personas`:** Tras el refactor de auth, la query de `personas` usaba `user.id` (inexistente); ahora usa `userId` + guard si falta.
- **Enrich company — prioridad navegador:** Si hay clave Apify (`api_keys` / `VITE_APIFY_API_TOKEN`), `sendSelectedToLeadsAndEnrich` ejecuta primero `enrichLeadCompaniesInBrowser` y solo si no enriqueció nada llama a Edge (evita POST 401 del gateway en Invocations). Doc: `APIFY_SETUP.md` (OPTIONS 200 + POST 401).
- **Saved searches — banner Send to leads:** Si el enrich falla, la UI solo muestra envío a Leads + “LinkedIn enrichment could not run” (sin detalle JWT ni comandos deploy ni “Refresh session”). Estilo warning (ámbar); éxito completo sigue en verde.
- **Pantalla gris / UI colgada (auth + Saved searches):** `useAuth` aplica un tope de 45s al estado `loading` y, si la URL traía `code`/`state` de OAuth y sigue bloqueado, hace `replaceState` sin query para poder recuperar la sesión de login. `SavedSearchesView` ya no devuelve `null` si el `viewingSearchId` apunta a una búsqueda borrada (mensaje + “Back to list”). `main.tsx` valida que exista `#root` antes de `createRoot`.
- **Saved searches → Send to leads + Invalid JWT (enrich):** Corregido bug donde, tras un enrich OK por secret, el `else { throw }` volvía a lanzar error. `invokeEnrichLeadCompaniesWithRetry` ya no hace `throw` en el 2.º 401: intenta `fetch` con `Authorization: Bearer <anon>` + `x-vloom-enrich-secret` + `{ leadIds, userId }`. Edge `enrich-lead-companies` / `enrich-lead-personas`: si el JWT de sesión falla pero el secret coincide, usan service role + `body.userId`. Docs: `APIFY_SETUP.md` + `.env.example` (`VLOOM_ENRICH_SECRET` / `VITE_VLOOM_ENRICH_SECRET`). Fallback navegador Apify si sigue fallando.
- **Post Feeds — mismo patrón que Jobs (JWT / navegador):** `runLinkedInPostFeedViaEdge` hace bypass a `runLinkedInPostFeedBrowser` si hay token Apify (`getApifyApiKeyForBrowser`); ante 401/403/Invalid JWT tras `invoke` intenta el modo navegador. `ApifyClient`: `runLinkedInPostSearch`, `runLinkedInHarvestProfileScraper` (autor + filtro `authorLocations`). Doc: `docs/APIFY_SETUP.md` (deploy `run-linkedin-post-feed --no-verify-jwt`).
- **Build Vercel (tsc) fix:** `src/lib/apify.ts` tenía error TS2339 (`data` inferido como `never` en select de `api_keys`). Se castea a `{ api_key_encrypted?: string } | null`. `npm run build` vuelve a pasar.
- **Zona horaria en nombres auto de Saved searches:** `run-job-search` y `run-linkedin-post-feed` formatean `autoName` en UTC-5 (tz `America/Bogota`) para que “Video Editor – …, HH:mm” coincida con negocio. Aplica a búsquedas nuevas (las existentes no cambian salvo renombrar).
- **Fix rápido — gateway 401 antes del handler:** Si Supabase Edge muestra POST 401 con `execution_id: null` (sin logs de `[run-job-search] request`), el deploy recomendado es `supabase functions deploy run-job-search --no-verify-jwt` (el `config.toml` puede no estar aplicándose según CLI).
- **Docs — logs para Invalid JWT:** En `docs/APIFY_SETUP.md` §3 se documenta dónde mirar (Dashboard Edge Functions → Logs, Network response, `supabase functions logs`, interpretación si no aparece `[run-job-search] request`).
- **Docs — JWT gateway vs función:** En `docs/APIFY_SETUP.md` §3 punto 5: alineación con “Securing Edge Functions” (verificación en gateway, `execution_id` null, `verify_jwt` / `resolveUserAndClient`, JWKS; consola `localhost:5173` = Vite no arrancado).

- **LinkedIn Jobs — sin aviso Apify en UI:** Eliminado bloque `LinkedInJobsApifyKeyNotice` en `SearchConfigPage`; la resolución de token sigue en `apify.ts` (`getApifyApiKeyForBrowser` / Edge).

- **LinkedIn Jobs — fallback si Edge devuelve 401:** `runJobSearchViaEdge` usa `getHttpResponseFromInvokeError` (por si `instanceof FunctionsHttpError` falla por bundles duplicados), parsea `msg`/`error` del body, y ante 401/403 o texto “Invalid JWT” llama a `runJobSearch` (Apify en navegador).

- **Invalid JWT en Edge:** `resolveUserAndClient` usa primero `auth.getClaims(jwt)` (verificación JWKS / claves asimétricas), luego `GET /auth/v1/user`, luego `getUser()`; import `@supabase/supabase-js@2.102.1` en el helper.
- **Invalid JWT — diagnóstico issuer:** `_shared/resolveUserAndClient.ts` compara `iss` del JWT con `SUPABASE_URL` de la Edge. `APIFY_SETUP.md` aclara que URL/anon en Edge son **secrets reservados** (no se pueden borrar); el arreglo típico es alinear `VITE_*` con Settings → API, nueva sesión y redeploy del frontend.
- **Edge Functions — «Invalid JWT» en el navegador:** Nuevo `_shared/resolveUserAndClient.ts`: si existe `SUPABASE_SERVICE_ROLE_KEY` en secrets, valida el access token con `auth.getUser(jwt)` en un cliente admin; si no, fallback a anon + `getUser()`. Integrado en `run-job-search`, `run-linkedin-post-feed`, `enrich-lead-companies`, `enrich-lead-personas`, `recompute-lead-scores`. Cliente: `invoke` con headers explícitos `Authorization` + `apikey` desde `refreshSession`. Doc en `APIFY_SETUP.md` (secret service_role).
- **Búsqueda (New Search) — `functions.invoke`:** `runJobSearchViaEdge` y `runLinkedInPostFeedViaEdge` dejan de usar `fetch` manual y pasan a `supabase.functions.invoke(...)`, que reutiliza el mismo `fetch` del cliente (apikey + JWT desde sesión). Reintento automático una vez en 401 tras `refreshSession`. Mensajes 401 incluyen el detalle devuelto por la función.
- **Edge Functions — validación de usuario:** En `run-job-search`, `run-linkedin-post-feed`, `enrich-lead-*` y `recompute-lead-scores` se reemplazó `auth.getUser(jwt)` por `auth.getUser()` sin argumentos, con el JWT solo en `global.headers.Authorization` (patrón Supabase). `getUser(jwt)` podía fallar con JWT asimétricos / migración de claves y devolvía 401 aunque el cliente enviara bien el token. **Despliegue:** `npx supabase functions deploy run-job-search run-linkedin-post-feed enrich-lead-companies enrich-lead-personas recompute-lead-scores`.
- **Edge Functions desde el cliente — header `apikey`:** Las llamadas `fetch` manuales a `functions/v1/*` solo enviaban `Authorization: Bearer <jwt>`; el gateway de Supabase (como `@supabase/supabase-js`) también requiere `apikey: <anon key>`. Sin eso el gateway puede responder 401 "Invalid JWT" aunque la sesión sea válida. Se centraliza `edgeFunctionPostHeaders` en `src/lib/apify.ts` y se usa en `run-job-search`, `run-linkedin-post-feed`, enrich companies/personas y `recompute-lead-scores`. Mensajes 401 actualizados (ya no culpan solo a `--no-verify-jwt`).
- **LinkedIn Jobs — filtro Workplace (Remote/Hybrid/On-site):** El actor `harvestapi/linkedin-job-search` espera `workplaceType` en minúsculas (`remote`, `hybrid`, `office`); la UI guardaba `Remote` / `Hybrid` / `On-site`, así que el filtro no aplicaba. Se añade `mapWorkplaceTypesToHarvestApi` en `src/lib/harvestLinkedInMaps.ts` (cliente `searchLinkedInJobs`) y copia en `supabase/functions/_shared/mapHarvestWorkplaceTypes.ts` (`run-job-search`) antes de enviar el body a Apify.
- **Saved searches — sin Autorun + badge New:** Eliminado checkbox Autorun, intervalo de re-ejecución y `useEffect` asociado en `SavedSearchesView`; solo **Run** manual. Tras cada run exitoso se guarda `scrapingJobId` en `localStorage` (`src/lib/savedSearchLatestImport.ts`); en resultados de una búsqueda guardada, `LeadsTable` recibe `highlightScrapingJobId` y muestra **New** al lado del nombre (misma fila) para leads con ese `scraping_job_id`. Migración `023_disable_autorun_all_saved_searches.sql` pone `autorun = false` en todas las filas.
- **LinkedIn Jobs — import asíncrono (webhook Apify):** Nueva Edge Function `apify-job-webhook` (`verify_jwt = false`, validación por `APIFY_WEBHOOK_SECRET` en query). Con `APIFY_WEBHOOK_SECRET` + `SUPABASE_SERVICE_ROLE_KEY` en Secrets, `run-job-search` arranca el actor con `waitForFinish=0` y webhooks ad-hoc (base64 en query); guarda `run_id`; responde `async: true`. El webhook importa el dataset con `SUPABASE_SERVICE_ROLE_KEY` y `_shared/linkedinJobImport.ts` (lógica compartida con el modo síncrono). `search_filters` incluye `excludeDomains` para el webhook. UI: mensaje “background import” en `SearchConfigPage` y `LeadsListView`. Documentado en `docs/APIFY_SETUP.md` §3b. Deploy: `npx supabase functions deploy run-job-search apify-job-webhook`.
- **Apify `READY` y normalización de estado:** Nuevo `supabase/functions/_shared/apifyStatus.ts` (`normalizeApifyRunStatus`, helpers). `run-job-search` y `run-linkedin-post-feed` normalizan el estado del run y del poll (evita fallos por casing). `enrich-lead-companies` y `enrich-lead-personas` aceptan `READY` y hacen poll como con `RUNNING` (antes `enrich-lead-companies` podía devolver `Apify run status: READY`). Cliente `ApifyClient` en `apify.ts`: `searchLinkedInJobs`, `searchIndeedJobs` y `enrichLinkedInProfile` entran en poll si el estado inicial es `READY`. **Despliegue:** `npx supabase functions deploy run-job-search run-linkedin-post-feed enrich-lead-companies enrich-lead-personas` (mismo `APIFY_API_TOKEN` en Secrets).
- **Vercel build (tsc):** `SavedSearchesView` autorun effect: `supabase` no se estrechaba dentro del callback async `tick` → TS18047. Se usa `const db = supabase` tras el guard y `db` en el update.
- **Migraciones: conflicto `018` duplicado:** Había dos archivos `018_*.sql` (lead status history + LinkedIn channel backfill); Supabase solo puede tener un `018` por remoto. El backfill de canal se renombró a `022_backfill_linkedin_job_post_channel.sql`; `db push` aplica `021` + `022` sin `--include-all` forzado.
- **Autorun, Leads/CRM visibilidad, dedupe Apify, lint:** (1) **Autorun** dejó de ser solo un flag: migración `021_autorun_last_run_at.sql`, `SavedSearchesView` guarda `autorun_last_run_at` tras cada Run manual o automático y, con la vista abierta, re-ejecuta búsquedas con autorun aprox. cada 24h (intervalo 15 min). (2) **Discovery → Leads** y **CRM** ya no filtran por defecto solo “marked as lead”: los imports de Apify (`is_marked_as_lead=false`) son visibles; el checkbox pasó a **Pipeline only (marked as lead)**. Preferencias CRM nuevas por defecto `marked_as_lead_only: false`. (3) **duplicate key `idx_leads_job_external_id`:** dedupe en Edge Functions con lectura paginada de `job_url`/`job_external_id` (`_shared/loadExistingLeadDedupeKeys.ts`); `saveJobsAsLeads` en `apify.ts` también pagina. (4) **Apify:** estados `READY` en cola se tratan como run en curso; si el poll agota el tiempo, error explícito en lugar de devolver 0 resultados en silencio. (5) **Lint:** `npm run lint` limpio (useAuth, CRMKanban, useLeads, KPITrackingView, enrich-lead-personas).
- **LinkedIn Jobs: canal, Remote y duplicate `job_external_id`:** (1) Migración `020_backfill_leads_channel_job_sources.sql` rellena `channel = 'LinkedIn Job Post'` (y Post Feeds donde aplica) donde estaba null; al enviar a Leads, `sendSelectedToLeadsAndEnrich` actualiza canal faltante en job posts de LinkedIn; el popup del lead muestra el canal inferido si la fila sigue vacía. (2) Filtro Remote/workplace: en `run-job-search` la ubicación "Remote" pasa a `workplaceType` y se quita de `locations`; los valores se mapean a `remote`/`hybrid`/`office` para HarvestAPI (ver bullet anterior si el mapeo cambió). (3) Dedupe al importar: por `job_external_id` además de `job_url`, y dedupe dentro del lote; `saveJobsAsLeads` alineado. (4) `enrich-lead-personas`: ignora URLs `/jobs/view/`, normaliza URL de contacto, dedupe con variantes con/sin trailing slash, ignora error único PostgreSQL `23505`.
- **CRM vs Saved searches → Send to leads:** El CRM cargaba hasta 500 filas **sin** filtrar por “marked as lead”, ordenadas por score; con muchos resultados de búsqueda sin marcar, los leads recién pasados a Leads podían quedar fuera de la primera página y parecer “ausentes”. `sendSelectedToLeadsAndEnrich` valida con `.select('id')` que el update afectó todas las filas. *Actualización posterior:* Discovery → Leads y CRM por defecto muestran **todos** los registros; el checkbox **Pipeline only (marked as lead)** acota al pipeline cuando hace falta.
- **Saved searches / Results: clic en fila = selección:** En `LeadsTable`, el clic en celdas llamaba a `onViewDetails`, que en Saved searches y en resultados de New Search era `noop`, así que **no se seleccionaba la fila**; el usuario podía creer que al hacer clic en el contacto ya estaba elegido y luego “Send to leads” actuaba sobre otra selección o ninguna. Ahora el clic en celdas de datos **alterna la selección** (mismo comportamiento que el checkbox) en `SavedSearchResultsTable` y `SearchResultsTable`.
- **Post Feeds + CRM “No name”:** El actor a veces no manda `companyName` estructurado; el headline del autor suele ser “Cargo en Empresa”. `run-linkedin-post-feed` ahora rellena `company_name` con más campos del JSON, infiere empresa desde el headline (`… at …`) y opcionalmente URL de compañía anidada en `author`. En el Kanban, `CRMCard` ya no usa solo `company_name` (mostraba “No name”): prioriza compañía, inferencia desde `contact_title`, contacto o extracto del post; segunda línea muestra headline/ubicación cuando aplica.
- **Post Feeds – Fix `job_posted_at` timestamp:** Ajustado `run-linkedin-post-feed` para normalizar `postedAt` cuando viene como objeto del actor (en vez de insertar `"[object Object]"` en `timestamp with time zone`).
- **Post Feeds – UI + filtro por ubicación del autor:** Ajustada la altura del textarea de keywords en `SearchConfigPage`, `authorLocations` ahora es un select multi (lista fija de ubicaciones) y el filtro se aplica scrapeando la ubicación del autor con `harvestapi/linkedin-profile-scraper` antes de insertar los posts (porque `linkedin-post-search` no entrega ubicación del autor en el output).
- **LinkedIn Post Feeds (Apify HarvestAPI):** Nuevo flujo de adquisición por posts: Edge Function `run-linkedin-post-feed` (actor `harvestapi/linkedin-post-search`) que auto-crea `saved_searches`, crea `scraping_jobs`, inserta resultados en `leads` con `channel = "LinkedIn Post Feeds"` e `is_marked_as_lead=false`, y deduplica por `job_external_id` (ID/URN del post) con fallback por `job_url`. Frontend: nueva fuente en `HomePage`, schema en `SearchConfigPage` (keywords + filtros), y `SavedSearchesView` ahora puede re-ejecutar búsquedas de Post Feeds además de Jobs. Wrapper `runLinkedInPostFeedViaEdge()` en `src/lib/apify.ts` y routing en `AppContent`.
- **Edge Functions – Evitar "Invalid JWT" sin flags:** Añadidos `config.toml` por función con `verify_jwt = false` en `run-job-search`, `enrich-lead-companies`, `enrich-lead-personas` y `recompute-lead-scores` para evitar que el gateway rechace JWTs tras migraciones de signing keys. Actualizados textos de ayuda en Saved Searches y docs (PASO_A_PASO, SUPABASE_SCHEMA, APIFY_SETUP) para desplegar sin `--no-verify-jwt` (dejándolo como fallback).
- **CRM y leads – Cinco mejoras:** (1) Nuevo lead desde el CRM se crea siempre en **Invite sent** y se inserta fila en `lead_status_history` para KPIs. (2) Campo **First contact date** (`first_contacted_at`): migración `019_leads_first_contacted_at.sql`, editable en Add lead (opcional) y en LeadCardPopup; usado en KPIs para asignar cohorte (manual prevalece sobre historial). (3) En el CRM, ordenación por **Date (A–Z)**, **Last contacted** (`updated_at`) y **First contacted** (`first_contacted_at`); columnas en tabla. (4) Leads importados desde LinkedIn Jobs (run-job-search + saveJobsAsLeads) tienen **channel = "LinkedIn Job Post"**; leads creados por enriquecimiento de personas (enrich-lead-personas) tienen **channel = "LinkedIn"**. (5) Nuevos canales en filtros y modales: **LinkedIn Job Post** y **LinkedIn Post Feeds**.
- **KPIs – Cohort y etapas para todos los contactados:** (1) Migración `018_backfill_lead_status_history_all_stages.sql`: backfill de todas las etapas del funnel (invite_sent, connected, reply, positive_reply, negotiation, closed, lost, disqualified) para leads marcados como lead que ya están en esa etapa o más adelante y no tenían fila en `lead_status_history` (movimientos pre-trigger o no registrados). Así los KPIs cuentan correctamente a todos los contactados. (2) En `kpiUtils.ts`, fallback por status actual: si un lead no tiene historial para una etapa, se considera que la alcanzó si su `status` actual es esa etapa o posterior (`hasEverBeenInStage` = historial o `hasReachedStageByCurrentStatus`). Con esto, los números (Connected, Reply, etc.) son correctos antes y después de ejecutar la migración.
- **KPIs – Excluir Disqualified completamente:** Leads en `disqualified` (por status actual o por historial) se excluyen del KPI tracking: no cuentan en Invite Sent, Connected, Replies, ni ninguna métrica del funnel (se consideran leads no contactados).
- **KPIs – Fallback de cohorte si falta historial:** Si un lead está en el funnel (invite_sent o más adelante) pero no tiene fila `to_status='invite_sent'` en `lead_status_history` (p. ej. movido antes del trigger), se incluye igual en KPIs atribuyendo la semana de cohorte a `updated_at` (o `created_at` si falta).
- **Backfill historial Invite Sent (migración 016):** Migración `016_backfill_lead_status_history_invite_sent.sql` inserta en `lead_status_history` una fila `to_status = 'invite_sent'` por cada lead que ya está en invite_sent o más adelante y no tenía esa fila (p. ej. movidos antes del trigger). Así los KPIs dejan de mostrar solo 4: tras aplicar la migración, todos esos leads tendrán historial y se contarán. `changed_at = COALESCE(lead.updated_at, lead.created_at)`.
- **KPIs – Reply/Connected/etc. por historial real:** Un lead en Disqualified o Lost no había pasado necesariamente por Reply; antes se usaba el status actual (hasReachedStatus) y se contaban mal. Ahora Connected, Reply, Positive reply, Negotiation, Closed, Lost y Disqualified se calculan con **lead_status_history**: solo se cuenta un lead en "Replies" si tiene al menos una fila con to_status = 'reply'. Hook `useStagesEverReachedByLead()` carga por cada lead_id el set de etapas por las que ha pasado; `computeKPIsByWeek` recibe `stagesEverReachedByLeadId` y usa `hasEverBeen(leadId, stage)` en lugar de status actual.
- **KPIs – Solo historial real (sin fallback updated_at):** Los números 79+31 Replies y 140+101 Invite Sent eran incorrectos porque el fallback con `updated_at` asignaba la semana por “última actualización” (cualquier cambio), inflando cohortes. Se quitó el fallback: solo se cuentan leads que tienen fila en `lead_status_history` (primera vez que se movieron a Invite Sent). Se vuelve a cargar para KPIs solo los leads cuyos id están en ese historial (con marked_as_lead y filtro canal). Así Invite Sent y Replies reflejan solo movimientos reales en el CRM.
- **KPIs – Fallback para leads sin historial + carga por funnel:** Si solo 4 aparecían en Invite Sent, era porque muchos movimientos ocurrieron antes del trigger de `lead_status_history` o el historial no los tenía. Cambios: (1) En `kpiUtils.ts`, `getPeopleContactedWeekKey` usa historial cuando existe; si no, **fallback a `lead.updated_at`** para leads con status invite_sent o superior, así se cuentan todos los que están en el funnel. (2) En KPIs ya no se cargan solo los leads que aparecen en el historial: se cargan **todos** los leads con `is_marked_as_lead` y status en [invite_sent, connected, reply, …]. Así la cifra de Invite Sent por semana refleja a todos (con semana correcta cuando hay historial, o semana de última actualización en fallback).
- **KPIs – Datos correctos por historial:** Los números de Invite Sent por semana salían mal porque los KPIs usaban solo los primeros 5000 leads (ordenados por score) de useLeads; muchos leads movidos a Invite Sent esta semana no estaban en esa ventana. Ahora la vista KPIs: (1) obtiene del historial (`lead_status_history`, to_status = invite_sent) todos los lead_id con su primera fecha; (2) carga solo esos leads (con `is_marked_as_lead` y filtro de canal), en chunks de 250. Así todas las compañías que han estado en Invite Sent entran en el cálculo y la semana correcta refleja cuántas se movieron esa semana. Hook `useLeadsForKPI(firstInviteSentByLeadId, filters.channel)` en KPITrackingView.
- **KPIs – Cohort solo por Invite Sent real:** Los números de "Invite sent" estaban inflados porque se usaba `created_at` como fallback para leads sin historial de Invite Sent. Ahora solo entran en el cohorte los leads que **alguna vez** se movieron a Invite Sent (según `lead_status_history`). Esos leads siguen en el mismo cohorte aunque la card se mueva después (Connected, Negotiation, etc.). Leads marcados como lead pero nunca movidos a Invite Sent ya no cuentan en ningún semana. `getPeopleContactedWeekKey` devuelve `string | null`; en `computeKPIsByWeek` se hace `if (key === null) continue`.
- **KPIs – Labels alineados con CRM + filtro por canal:** En la vista KPIs, las etiquetas coinciden con el CRM: "Invite sent (Companies)" (antes People contacted), "Connected (accepted) · Companies", "Negotiation (Companies)" (antes Opportunity). Todas las filas indican "Companies" y "rate vs Invite sent". Añadido filtro **Channel** (multi-select) en la cabecera de la vista para ver KPIs por uno o varios canales; al filtrar, los leads usados en el cálculo son solo los del canal seleccionado. Texto de ayuda y modal ("No companies") actualizados. `KPITrackingView` usa `filters` y `updateFilter` de `useLeads` para el filtro de canal.
- **KPIs – Lista de personas por número:** En la vista KPIs, cada celda numérica es clicable: al hacer clic se abre un modal con la lista de personas (contacto · compañía) que componen ese número para esa semana y métrica. `WeekKPI` en `kpiUtils.ts` incluye arrays por métrica (`peopleContactedLeads`, `connectedLeads`, etc.); `KPITrackingView` usa `KpiCell` con popover/modal para mostrar la lista. Texto de ayuda actualizado: "Click a number to see the list of people".
- **Persona enrichment on each card/lead:** Edge Function `enrich-lead-personas` (harvestapi/linkedin-company-employees): por cada compañía distinta de los leads seleccionados, ejecuta el actor con la URL de la compañía y los filtros de las Personas activas; crea **un lead por persona** encontrada (misma compañía, distinto contacto), deduplica por `contact_linkedin_url`, crea tarea "Contact …" por cada nuevo lead. Cliente: tras "Send to leads" se llama a esta función (no bloqueante); en CRM (vista tabla) con leads seleccionados aparece "Enrich with personas" y "Delete selected". `enrichLeadsWithPersonas(leadIds)` en `apify.ts`. Desplegar con `npx supabase functions deploy enrich-lead-personas` (usa el mismo `APIFY_API_TOKEN` que enrich-lead-companies).
- **Vercel build fix (CRMKanban):** El último deploy fallaba con `npm run build` exit 2 por TS6133: `draggedLead` declarado pero no leído en `CRMKanban.tsx`. Corregido renombrando a `_draggedLead`; el build pasa y el redeploy en Vercel debería funcionar.
- **Personas – Function, Seniority, Locations as options:** En la pestaña Personas, los campos Function y Seniority son ahora selects con opciones predefinidas (HR, Engineering, Marketing, Sales, etc.; Intern, Manager, Director, VP, etc.). Locations pasa a ser un multi-select con lista fija (USA, Canada, Remote, UK, Europa, LATAM, APAC). Se mantiene compatibilidad con valores custom ya guardados (se muestran en el dropdown al editar).
- **Personas (tab + API):** Nueva pestaña "Personas" en el sidebar debajo de Tasks. Los usuarios pueden definir perfiles objetivo (nombre, función, seniority, job title keywords, locations, max items, profile scraper mode, activo) para el enriquecimiento de personas con `harvestapi/linkedin-company-employees`. Migración `014_personas.sql` (tabla `personas` + RLS), tipos `Persona` y `ProfileScraperMode` en `database.ts`, hook `usePersonas` (get/create/update/delete), componente `PersonasView` con listado y modal add/edit/delete. La URL de compañía se tomará de cada registro al ejecutar el enriquecimiento (no se guarda en la persona).
- **KPI tracking (CRM):** Vista "KPIs" en el sidebar con tabla de KPIs por semana (lunes–domingo). Todas las métricas se atribuyen a la **semana del primer contacto** del lead (`created_at`), no a la semana en que se movió la tarjeta. Métricas: People contacted, % accepted (connected), Replies, Positive replies, Opportunity, Closed, Lost, Disqualified. Utilidades en `src/lib/kpiUtils.ts` (getWeekStart/getWeekKey, computeKPIsByWeek), componente `src/components/KPIs/KPITrackingView.tsx`, integrado en AppContent en lugar del placeholder.
- **CRM – Add new leads + Channel field:** Los usuarios pueden añadir leads manualmente desde el CRM ("Add lead") para usar la app como CRM principal. Modal con Company, Contact name, Email, Channel y Notes; el lead se crea como "marked as lead" y se genera la tarea "Contact …". Nuevo campo **Channel** en leads (migración `013_leads_channel.sql`) para indicar el canal de origen (LinkedIn, Website, Referral, Event, Cold outreach, Email, Other). Channel visible y editable en LeadCardPopup, FilterBar (filtro por canal), LeadsTable (columna) y CRMCard (Kanban).
- **CRM, Saved searches, Enrichment (bugs/changes):** (1) CRM Kanban: columna Disqualified ahora acepta soltar tarjetas (min-height en zona de drop + flex-shrink-0 en header) para que incluir una card en Disqualified funcione. (2) Saved searches: eliminado el botón y formulario "New saved search"; las búsquedas se crean solo al ejecutar desde New Search. Mensaje vacío actualizado. (3) Enrichment: la Edge Function `enrich-lead-companies` ignora leads que ya tienen `last_enriched_at`; solo se envían a Apify y se re-enriquecen los que aún no tienen enriquecimiento, evitando re-enriquecer duplicados al volver a ejecutar una búsqueda.
- **Deploy Vercel (Build Failed):** Errores de TypeScript en Vercel (`npm run build` exit 2) por tipos de Supabase: `insert`/`update` en tablas `tasks`, `saved_searches`, `leads`, `scraping_jobs` inferían `never`. Corregido con aserciones `as never` en CRMCard, useLeads, useSavedSearches, useTasks, apify.ts, sendgrid.ts y SearchConfigPage. `npm run build` pasa; redeploy en Vercel debería funcionar.
- **Saved Searches – Disqualified view y Restore:** En resultados de saved search, filtro por defecto excluye `disqualified`. Junto a "Recalculate scores" y "Refresh" se añadió el enlace "Disqualified" que cambia la vista a solo leads descalificados; en esa vista aparece "Back to results" y, con filas seleccionadas, la acción "Restore to Backlog" (y barra superior con el mismo botón). El mensaje al marcar como disqualified ya no muestra `[object Object]` (siempre string).
- **CRM – Columnas con drag handle:** En LeadsTable el selector de columnas usa icono de tres rayas (GripVertical) para reordenar por arrastre en lugar de flechas arriba/abajo.
- **Saved searches: renombrado sin duplicados:** Quitado “Save this search” (creaba una búsqueda nueva aunque el run ya se auto-guardaba). Ahora cada run devuelve `savedSearchId`/`savedSearchName` desde la Edge Function y se puede **renombrar inline** el saved search creado automáticamente desde `SearchConfigPage`. En `SavedSearchesView` se arregla el bug de espacio al renombrar (se eliminó el `input` anidado dentro de un `button`, que activaba navegación al presionar Space).
- **Mejoras UI (Kanban, Discovery, Search):** (1) CRM Kanban: columnas más anchas (`w-64`, `min-w-[14rem]`) para menos scroll; tarjetas con nombre de compañía una sola vez, segunda línea con ubicación, puesto, contacto o industria en tipografía más pequeña (`text-[11px]`). (2) HomePage / fuentes: quitados los identificadores Apify de cada card y el footer "Apify Actors · Data in Supabase". (3) SearchConfigPage: quitados el bloque "Estimated cost" y el panel "These fields are sent to the job source..."; quitado el `apifyActorId` del encabezado de la página de búsqueda.
- **Login obligatorio en producción:** En producción, si Supabase no está configurado (env vars no disponibles en el build) ya no se muestra la app sin auth: se muestra una pantalla "Configuración requerida" con el hint de variables. Solo en desarrollo se permite entrar sin auth cuando no hay Supabase (preview local). Así, en producción siempre se exige iniciar sesión salvo que las env vars falten (en ese caso no se puede usar la app hasta configurar y redesplegar).
- **Build de producción corregido:** Errores de TypeScript que impedían el deploy resueltos: `src/vite-env.d.ts` para `import.meta.env`, imports React no usados eliminados, GoogleIcon con prop `className`, `updateSavedSearch` acepta `name`, parámetros no usados con prefijo `_`, LeadsTable sort key como `keyof Lead`, guards y aserciones para `supabase` posible null, cliente Supabase tipado como `SupabaseClient<any>` para que insert/update acepten payloads. `npm run build` pasa; Vercel deploy puede ejecutarse.
- **Salida a producción:** Guía completa en `docs/DEPLOY.md`: recomendación (Vercel preferido, Netlify alternativa), checklist previo, paso a paso para Vercel y Netlify, configuración Supabase en producción. Añadidos `vercel.json` y `netlify.toml` con build command, output `dist` y redirects SPA.
- **Tasks al marcar job post como lead:** Al incluir un Job Post como lead (`is_marked_as_lead: true`) se crea automáticamente una tarea tipo "Contactar a {empresa} – {contacto}" vinculada al lead. Migración `011_tasks.sql` (tabla `tasks` con `lead_id`, `title`, `status`). Hook `useTasks`, vista Tasks con lista de tareas y botón "Ver tarjeta" que lleva al CRM. La tarjeta del trabajo queda conectada vía `lead_id`.
- **Sesión caducada tras subir JWT expiry:** Documentada la causa (migración Legacy JWT → JWT Signing Keys; el gateway de Edge Functions puede rechazar el token). Solución: desplegar `run-job-search` con verificación JWT del gateway desactivada (vía config por función o `--no-verify-jwt`). La función sigue validando el JWT con `getUser(jwt)`. Actualizados PASO_A_PASO (0c), SUPABASE_SCHEMA (sección 401/sesión), mensajes de error en `apify.ts` y patrón del botón "Actualizar sesión" en SearchConfigPage.
- **Eliminación de teams:** Migración `008_remove_teams.sql`: se elimina la tabla `teams` y todas las columnas `team_id` (profiles, leads, scoring_presets, email_templates, scraping_jobs, saved_searches, api_keys). `api_keys` pasa a ser por usuario (`user_id`). RLS simplificado a solo usuario (auth.uid()). Código: tipos sin Team/team_id, getCurrentTeam eliminado, apify/sendgrid/ai-email usan user_id para api_keys, Edge Function sin teamId, UI "Share with Team" → "Share", "Show Team Leads" → "Show shared leads". Ver `docs/TEAM_FLOW.md`.
- **Team y Saved searches:** Documentado en `docs/TEAM_FLOW.md` qué es un team, para qué sirve y el flujo actual. Migración `007_saved_searches_optional_team_and_backfill.sql`: `saved_searches.team_id` pasa a ser opcional; nueva política RLS para ver propias o del equipo; backfill que crea un `saved_search` por cada una de las **últimas 3** `scraping_jobs` sin `saved_search_id` por usuario y enlaza el job. Edge Function y frontend permiten crear/auto-guardar con `team_id` null.
- **Navegación Saved searches:** Al hacer clic en &quot;Saved searches&quot; desde New Search (SearchConfigPage), ahora se muestra la lista: al navegar se pone `view = 'app'` para que el contenido principal sea la sección elegida y no se quede en la página de búsqueda.
- **Guardar todas las búsquedas:** Cada ejecución desde New Search se guarda automáticamente en Saved searches (Edge Function crea un `saved_search` con nombre auto, ej. &quot;Video Editor – 23 Feb 2025, 15:42&quot;, y asocia el `scraping_job`). Solo si el usuario tiene equipo; si no, el run sigue sin `saved_search_id`. El botón &quot;Save search&quot; sigue permitiendo guardar con nombre personalizado para re-ejecutar.
- **Results (0) tras importar leads:** Tabla de resultados no se actualizaba con el `scrapingJobId` del run recién terminado. Se corrige con: (1) `key={scrapingJobId}` en `SearchResultsTable` para montar una instancia nueva por búsqueda; (2) en `useLeads`, efecto que sincroniza `scraping_job_id` y `saved_search_id` desde `initialFilters` cuando cambian, para que la query use siempre el id correcto.
- **Apify API docs alignment:** Edge Function y `src/lib/apify.ts` alineados con la documentación oficial: autenticación con `Authorization: Bearer` (sin token en URL), `waitForFinish` solo como query param (máx. 60s), body solo con input del Actor; normalización de actorId a formato tilde (`username~actor-name`) en las URLs a api.apify.com; polling en el cliente (searchLinkedInJobs, searchIndeedJobs, enrichLinkedInProfile) hasta SUCCEEDED/FAILED con timeout; parsing de errores Apify (`error.message`) para mensajes más claros.
- **Sidebar + LinkedIn Jobs:** SearchConfigPage se muestra dentro de AppLayout (no reemplaza la app), así el panel lateral sigue visible al abrir "LinkedIn Jobs" en Discovery.
- **Copy en inglés:** Toda la comunicación visible en la plataforma pasada a inglés (FilterBar: By people/companies, Leads only; CRMView: Table/Kanban, Marked leads only, Add VITE_SUPABASE…; CRMCard/LeadsTable: Mark as lead / Remove from leads, No name; CRMKanban: Loading…).
- **Supabase opcional:** No lanzar si faltan env vars; app renderiza siempre.
- **Tema oscuro Vloom:** Paleta dark, Inter, colores ideas.wearevloom.com.
- **Error Boundary:** Errores no capturados muestran mensaje en vez de pantalla en blanco.
- **Lazy load Dashboard:** Home carga sin importar useLeads/Supabase.
- **Estructura docs:** BACKLOG, PRE_MORTEMS, DEBT, PROCESS.
- **Lint:fix + pre-commit:** Script y hooks para lint automático.
- **GitHub setup:** .gitignore, docs/GITHUB_SETUP.md con pasos para conectar y sincronizar.

---

## Pendiente de limpieza

- [x] Corregir errores de TypeScript para que `npm run build` pase: hecho (vite-env.d.ts, supabase client any, guards, aserciones).
- [ ] Revisar imports no usados en componentes.
- [ ] Unificar clases Tailwind duplicadas (vloom-* vs gray-* residuales).
- [ ] Verificar que `supabase/migrations/001_initial_schema.sql` existe y está actualizado.
- [ ] Añadir `SearchConfigPage`, `HomePage`, `ErrorBoundary` a la estructura en README.
- [x] Resolver 14 warnings de ESLint (imports no usados, deps en hooks): corregidos.

---

## Deuda técnica

Ver [docs/DEBT.md](DEBT.md) para el registro formal (Problema | Solución | Deadline).
