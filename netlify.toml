# Leadflow Vloom – Netlify
# https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback (por si más adelante usas rutas en URL)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
